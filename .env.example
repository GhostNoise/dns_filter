# AdGuard Home admin credentials
AGH_USER=admin
AGH_PASSWORD=changeme

# DuckDNS — needed only for DoH profile (docker compose --profile doh up -d)
# DUCKDNS_TOKEN=your-token-here
# DUCKDNS_DOMAIN=yourname.duckdns.org
